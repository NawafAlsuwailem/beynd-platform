#!/usr/bin/env node

/**
 * Automatic Commitizen + Husky setup (v9+ compatible)
 * ---------------------------------------------------
 * - Runs automatically after `npm install` in consuming services.
 * - Ensures Husky is installed and properly configured.
 * - Always (re)creates the prepare-commit-msg hook with the latest template.
 * - Fully automated — no manual developer steps required.
 */

const { execSync } = require('child_process');
const path = require('path');
const fs = require('fs');

const projectRoot = process.env.INIT_CWD || path.resolve(__dirname, '../../');
const huskyDir = path.join(projectRoot, '.husky');
const hookPath = path.join(huskyDir, 'prepare-commit-msg');

console.log('🧠 Initializing Commitizen + Husky setup...');

try {
    process.chdir(projectRoot);

    // Skip if not a Git repo
    if (!fs.existsSync(path.join(projectRoot, '.git'))) {
        console.log('⚠️  Skipping Husky setup (no .git directory found).');
        process.exit(0);
    }

    // 1️⃣ Ensure Husky is installed
    if (!fs.existsSync(huskyDir)) {
        console.log('⚙️  Installing Husky...');
        execSync('npx husky install', { stdio: 'inherit' });
    }

    // 2️⃣ Ensure Git hook path is set
    execSync('git config core.hooksPath .husky', { stdio: 'inherit' });

    // 3️⃣ Always (re)create prepare-commit-msg hook
    console.log('🪝 Updating prepare-commit-msg hook...');
    const hookContent = `#!/bin/sh

# Interactive Commitizen prompt (Husky v9+ safe)
exec < /dev/tty && npx cz --hook || true
exit 0
`;
    fs.writeFileSync(hookPath, hookContent);
    fs.chmodSync(hookPath, 0o755);

    console.log('🎉 Commitizen + Husky are ready! Run `git commit` to open the interactive prompt.');
} catch (err) {
    console.error('❌ Commitizen setup failed:', err.message);
    process.exit(0); // don’t break npm install
}
